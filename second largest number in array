#include<stdio.h>
int main()
{
int size;
scanf("%d",&size);
int a[size];
for(int i=0;i<size;i++)
{
scanf("%d",&a[i]);
}
int max=a[0];
int sl=a[0];
for(int i=1;i<size;i++)
{
if(a[i]>max)
{
sl=max;
max=a[i];
}
else if(a[i]>sl && sl!=max)
{
 sl=a[i];   
}
}
printf("%d",sl);
return 0;
}
