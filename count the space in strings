#include<stdio.h>
#include<string.h>
int main()
{
char ch[20];
int count=0;
fgets(ch,20,stdin);
for(int i=0;i<strlen(ch);i++)
    {
        if(ch[i]==' ')
        {
    count++;
}
}
printf("%d",count);
return 0;
}
