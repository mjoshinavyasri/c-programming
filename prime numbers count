#include<stdio.h>
int main () {
    int n,i,j,number=0;
    scanf("%d",&n);
    for (i=2;i<=n;i++) {
        int count=0;
        for (j=1;j<=i;j++){
            if (i%j==0){
                count++;
            }
        }
        if (count==2){
        number++;
    }
}
    printf("%d",number);
    return 0;
}
