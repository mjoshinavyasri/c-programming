#include<stdio.h>
int main() {
    int month,fees;
    scanf("%d",&month);
    switch (month){
        case 1:
    fees=1500;
    break;
    case 3:
    fees=4000;
    break;
    case 6:
    fees=7000;
    break;
    default:
    printf("invalid month");
}
    printf("%d",fees);
    return 0;
}
