#include <stdio.h>
#include<string.h>
int main() {
    int i,j,temp;
   char ch[50];
   scanf("%s",ch);
   for(i=0;i<ch[i];i++)
   {
  for(j=i+1;i<ch[j];j++)
  {
     if (ch[i]>ch[j])
     {
      temp=ch[i];
      ch[i]=ch[j];
      ch[j]=temp;
  }
  }
   }
   printf("%s",ch);
    return 0;
}
