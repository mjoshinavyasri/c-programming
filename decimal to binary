#include <stdio.h>
int main() {
    int b,d=0;
    int base=1;
    scanf("%d",&b);
    while(b>0)
    {
    int rem=b%2;
     d=d+rem*base;
    base=base*10;
    
      b=b/2;
    }
       printf("%d ",d);
    return 0;
}
