#include<stdio.h>
int main() {
   int plan,paymentmode,cashback;
    scanf("%d%d",&plan,&paymentmode);
    switch (plan) {
        case 1:
            if (paymentmode==11) {
                cashback=199-20;
            }
            else if (paymentmode==13) {
                cashback=199;
            }
            break;
        case 2:
            if (paymentmode==11) {
                cashback=399-20;
            }
            else if (paymentmode==13) {
                cashback=399;
            }
            break;
    }
    printf("%d",cashback);
    return 0;
}
