bool isAnagram(char* s, char* t) {
  if(strlen(s)!=strlen(t)){
    return false;
  }  
  int freq[127]={0};
  for(int i=0;s[i];i++){
    freq[s[i]]++;
    freq[t[i]]--;
  }
  for(int i=0;i<127;i++){
    if(freq[i]!=0){
        return false;
    }
}
return true;
}
