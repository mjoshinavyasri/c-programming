#include<stdio.h>
int main() {
    int a,b,c;
    scanf("%d%d%d",&a,&b,&c);
    int sum=a+b+c;
    if (sum==180) {
        printf("valid triangle");
    }
    else {
        printf("invalid triangle");
    }
    return 0;
}
