#include<stdio.h>
int main()
{
    int sum=0;
int n;
    scanf("%d",&n);
for(int i=1;i<n;i++)
    {
     if(n%i==0)
        sum+=i;
    }
    if(sum==n)
    {
        printf("perfect number");
    }
    else
    {
        printf("not perfect number");
    }
return 0;
}
