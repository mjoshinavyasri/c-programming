#include<stdio.h>
int main() {
   int speedtype,amount,deliverycharge;
    scanf("%d%d",&speedtype,&amount);
    switch (speedtype) {
        case 1:
deliverycharge=50
break;
        case 2:
if (amount<1000) {
    deliverycharge=100;
break;
}
else if (amount>=1000) {
    deliverycharge=0;
}
            break;
    }
    printf("%d",deliverycharge);
    return 0;
}
