#include<stdio.h>
int main() {
    int class,weight,charge;
    scanf("%d%d",&class,&weight);
    switch (class) {
        case 1:
          charge=weight*300;
            printf("%d",charge);
    break;
    case 2:
            if (weight==3) {
                printf("free");
            }
                else{
                    charge=(weight-3)*300;
                    printf("%d",charge);
            }
            break;
}
    return 0;
}
