#include<stdio.h>
int main() {
    int type,minute,charge;
    scanf("%d%d",&type,&minute);
    switch (type) {
        case 1:
            charge=minute*1;
            break;
        case 2:
            charge=minute*3;
            break;
    case 3:
    charge=minute*10;
    break;
}
    printf("%d",charge);
    return 0;
}
