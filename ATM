#include<stdio.h>
int main() {
    int n;
    scanf("%d450",&n);
    if (n%100==0)
        {
        printf("valid amount");
        }
    else {
        printf("invalid amount");
    }
    return 0;
}
