#include<stdio.h>
int main() {
   int plan;
    float data;
    scanf("%d%f",&plan,&data);
   switch (plan) {
       case 1:
           if (data<=1.0) {
              printf("normal speed");
           }
         else if (data>1.0)
         {
               printf("speed reduced");
           }
           break;
       case 2:
           if(data<=2.0) {
               printf("normal speed");
           }
           else if (data>2.0)
           {
               printf("extra charges applied");
           }
           break;
   }
    return 0;
}
