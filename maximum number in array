#include<stdio.h>
int main()
{
int size;
scanf("%d",&size);
int a[size];
int max=a[0];
for(int i=0;i<size;i++)
{
    scanf("%d",&a[i]);
}
for(int i=0;i<size;i++)
{
    if(a[i]>max)
    {
    max=a[i];
    }
}
printf("%d",max);
return 0;
}
