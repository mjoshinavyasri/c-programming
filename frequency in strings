#include <stdio.h>
#include<string.h>
int main() {
   int n[256]={0};
   int i;
 char ch[50];
 fgets(ch,50,stdin);
 for(i=0;i<ch[i];i++)
 {
     n[ch[i]]++;
 }
  for(int i=0;i<256;i++)
 {
   if(n[i]!=0 && i!='\n')
   {
        printf("%c=%d\n",i,n[i]);
   }
 }
    return 0;
}
