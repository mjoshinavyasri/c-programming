#include<stdio.h>
int main() {
    int category,distance;
    scanf("%d%d",&category,&distance);
    switch (category) {
        case 1:
          if (distance<=30) {
              printf("eligible");
          }
            else if (distance>30) {
                printf("eligible with extra fee");
            }
    break;
    case 2:
            if (distance<=30) {
                printf("eligible");
            }
            else if (distance>30) {
                printf("not eligible");
            }
            break;
}
    return 0;
}
